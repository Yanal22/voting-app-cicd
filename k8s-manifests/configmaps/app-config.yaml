apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: voting-app
  labels:
    app: voting-system
data:
  # Redis configuration
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"
  
  # PostgreSQL configuration
  POSTGRES_HOST: "postgres"
  POSTGRES_PORT: "5432"
  POSTGRES_DB: "postgres"
