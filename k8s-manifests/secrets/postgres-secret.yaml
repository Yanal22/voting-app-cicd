apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: voting-app
  labels:
    app: voting-system
type: Opaque
stringData:
  # Using stringData (not base64) for readability
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "postgres123"
